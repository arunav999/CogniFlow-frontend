import { pageTitle } from "../../utils/utils";

const TicketsBoard = () => {
  pageTitle("Tickets Board");

  return (
    <>
      <div className="">Tickets Board</div>
    </>
  );
};

export default TicketsBoard;
